# Virtual Listening Room — Phase Check

A single-file browser tool for simulating a virtual listening room and inspecting
speaker phase behaviour, including air and wall absorption (Stage 4).

## Usage

Open `index.html` in a modern browser. No build step, no dependencies.

## Contents

- `index.html` — the entire application (markup, styles, and scripts inline).
